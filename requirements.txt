# Redis version pin - redis 5.1+ breaks Celery's redis.client.Pipeline
redis>=5.0.0,<5.1.0

# SQL Server connectivity
pyodbc>=5.0.0
pymssql>=2.2.0
apache-airflow-providers-odbc>=4.0.0
apache-airflow-providers-microsoft-mssql>=3.0.0

# PostgreSQL connectivity
psycopg2-binary>=2.9.0
apache-airflow-providers-postgres>=5.0.0

# Common SQL provider (used by SQLExecuteQueryOperator)
apache-airflow-providers-common-sql>=1.0.0

# Git operations (for git sync)
GitPython>=3.1.0

# dbt Core and adapters
dbt-core>=1.9.0
dbt-postgres>=1.9.0
dbt-sqlserver>=1.8.0
airflow-dbt-python>=2.0.0

# S3/MinIO connectivity
boto3>=1.35.0
apache-airflow-providers-amazon>=9.0.0

# OpenMetadata lineage integration
# NOTE: openmetadata-ingestion requires SQLAlchemy<2, which conflicts with Airflow 3.x
# Install separately or use OpenMetadata's standalone ingestion container instead
# openmetadata-ingestion>=1.6.0

# dlt-hub for data loading
dlt[postgres,mssql,filesystem]>=0.4.0

# Fake data generation
faker>=28.0.0

# Common data processing libraries
pandas>=2.0.0
polars>=1.0.0

# HTTP and API utilities
requests>=2.31.0
httpx>=0.27.0

# Database migrations
alembic>=1.14.0
SQLAlchemy>=2.0.0
